/*4.77: You can use if/else statements to do a boolean comparison. The simplified version looks like this: */

function isLess(a, b) {
  // Fix this code
  return (a <= b);

}

// Change these values to test
isLess(10, 15);
