/*4.57: Comparison operators return boolean values. The equality operator(==) looks at two values and returns true if they are equal or false if they are not.  */

// Setup
function testEqual(val) {
  if (val==12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(12);
