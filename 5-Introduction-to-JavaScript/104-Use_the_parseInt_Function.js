/*4.104: parseInt() looks at a string and changesit into an integer. If it can't be changed into number it returns NaN(Not a Number). */

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
